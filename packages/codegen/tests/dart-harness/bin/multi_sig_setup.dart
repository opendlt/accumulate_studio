/// Multi-Signature Setup
/// Generated by Accumulate Studio
/// Network: kermit

import 'dart:async';
import 'dart:convert';
import 'dart:io';
import 'dart:typed_data';
import 'package:crypto/crypto.dart';
import 'package:opendlt_accumulate/opendlt_accumulate.dart';

const String defaultV2 = 'https://kermit.accumulatenetwork.io/v2';
const String defaultV3 = 'https://kermit.accumulatenetwork.io/v3';

String toHexString(List<int> bytes) {
  return bytes.map((b) => b.toRadixString(16).padLeft(2, '0')).join();
}

Future<void> main() async {
  final v2 = Platform.environment['ACCUMULATE_V2_URL'] ?? defaultV2;
  final v3 = Platform.environment['ACCUMULATE_V3_URL'] ?? defaultV3;
  final client = Accumulate.custom(v2Endpoint: v2, v3Endpoint: v3);
  final txIds = <MapEntry<String, String>>[];
  try {
    // Flow variables (customize these for your use case)
    final adi_name = 'adi-${DateTime.now().millisecondsSinceEpoch ~/ 1000}';  // Name for your ADI (e.g., 'my-multisig-adi')


    // =========================================================
    // Generate Keys (Signer 1) (GenerateKeys)
    // =========================================================
    final generateKeysSigner_1Kp = await Ed25519KeyPair.generate();
    final generateKeysSigner_1Key = UnifiedKeyPair.fromEd25519(generateKeysSigner_1Kp);
    final generateKeysSigner_1Lid = await generateKeysSigner_1Kp.deriveLiteIdentityUrl();
    final generateKeysSigner_1Lta = await generateKeysSigner_1Kp.deriveLiteTokenAccountUrl();
    final generateKeysSigner_1PubKey = await generateKeysSigner_1Kp.publicKeyBytes();
    final generateKeysSigner_1PubHash = toHexString(sha256.convert(generateKeysSigner_1PubKey).bytes);
    print('Generated keypair:');
    print('  Lite Identity: ${generateKeysSigner_1Lid}');
    print('  Lite Token Account: ${generateKeysSigner_1Lta}');
    print('  Public Key Hash: ${generateKeysSigner_1PubHash}');


    // =========================================================
    // Request Tokens (Faucet)
    // =========================================================
    print('Requesting tokens from faucet...');
    for (var i = 0; i < 3; i++) {
      try {
        final resp = await client.v2.faucet({
          'type': 'acmeFaucet',
          'url': generateKeysSigner_1Lta.toString(),
        });
        print('  Faucet ${i + 1}/3: ${resp["txid"]}');
      } catch (e) {
        print('  Faucet ${i + 1}/3 failed: $e');
      }
      await Future.delayed(Duration(seconds: 2));
    }
    print('Faucet request completed');


    // =========================================================
    // Wait for ACME (WaitForBalance)
    // =========================================================
    print('Waiting for balance >= 15.00000000...');
    final waitForAcmeMinBal = BigInt.parse('15.00000000'.replaceAll('.', ''));
    for (var i = 0; i < 30; i++) {
      try {
        final result = await client.v3.rawCall('query', {
          'scope': generateKeysSigner_1Lta.toString(),
          'query': {'queryType': 'default'},
        });
        final balance = result['account']?['balance'];
        if (balance != null && BigInt.parse(balance.toString()) >= waitForAcmeMinBal) break;
      } catch (_) {}
      await Future.delayed(Duration(seconds: 2));
    }
    print('Balance reached');


    // =========================================================
    // Add Credits (Action: AddCredits)
    // =========================================================
    final addCreditsOracle = await client.v3.rawCall('network-status', {});
    final addCreditsOraclePrice = addCreditsOracle['oracle']['price'] as int;
    final addCreditsSigner = SmartSigner(
      client: client.v3,
      keypair: generateKeysSigner_1Key,
      signerUrl: generateKeysSigner_1Lid.toString(),
    );
    final addCreditsResult = await addCreditsSigner.signSubmitAndWait(
      principal: generateKeysSigner_1Lta.toString(),
      body: TxBody.addCredits(
        recipient: generateKeysSigner_1Lid.toString(),
        amount: '1000000000',
        oracle: addCreditsOraclePrice,
      ),
      memo: 'Add credits',
      maxAttempts: 30,
    );
    if (addCreditsResult.success) {
      print('AddCredits SUCCESS - TxID: ${addCreditsResult.txid}');
      if (addCreditsResult.txid != null) {
        txIds.add(MapEntry('AddCredits', addCreditsResult.txid!));
      }
    } else {
      print('AddCredits FAILED: ${addCreditsResult.error}');
    }


    // =========================================================
    // Wait for Credits (WaitForCredits)
    // =========================================================
    print('Waiting for credits >= 1000...');
    for (var i = 0; i < 30; i++) {
      try {
        final result = await client.v3.rawCall('query', {
          'scope': generateKeysSigner_1Lid.toString(),
          'query': {'queryType': 'default'},
        });
        final creditBalance = result['account']?['creditBalance'];
        if (creditBalance != null && (creditBalance as int) >= 1000) break;
      } catch (_) {}
      await Future.delayed(Duration(seconds: 2));
    }
    print('Credits reached');


    // =========================================================
    // Create ADI (Action: CreateIdentity)
    // =========================================================
    final createAdiUrl = 'acc://${adi_name}.acme';
    final createAdiSigner = SmartSigner(
      client: client.v3,
      keypair: generateKeysSigner_1Key,
      signerUrl: generateKeysSigner_1Lid.toString(),
    );
    final createAdiResult = await createAdiSigner.signSubmitAndWait(
      principal: generateKeysSigner_1Lta.toString(),
      body: TxBody.createIdentity(
        url: createAdiUrl,
        keyBookUrl: '${createAdiUrl}/book',
        publicKeyHash: generateKeysSigner_1PubHash,
      ),
      memo: 'Create ADI',
      maxAttempts: 30,
    );
    if (createAdiResult.success) {
      print('CreateIdentity SUCCESS - TxID: ${createAdiResult.txid}');
      if (createAdiResult.txid != null) {
        txIds.add(MapEntry('CreateIdentity', createAdiResult.txid!));
      }
    } else {
      print('CreateIdentity FAILED: ${createAdiResult.error}');
    }


    // =========================================================
    // Credit Key Page (Action: AddCredits)
    // =========================================================
    final creditKeyPageOracle = await client.v3.rawCall('network-status', {});
    final creditKeyPageOraclePrice = creditKeyPageOracle['oracle']['price'] as int;
    final creditKeyPageSigner = SmartSigner(
      client: client.v3,
      keypair: generateKeysSigner_1Key,
      signerUrl: generateKeysSigner_1Lid.toString(),
    );
    final creditKeyPageResult = await creditKeyPageSigner.signSubmitAndWait(
      principal: generateKeysSigner_1Lta.toString(),
      body: TxBody.addCredits(
        recipient: '${createAdiUrl}/book/1',
        amount: '1000000000',
        oracle: creditKeyPageOraclePrice,
      ),
      memo: 'Add credits',
      maxAttempts: 30,
    );
    if (creditKeyPageResult.success) {
      print('AddCredits SUCCESS - TxID: ${creditKeyPageResult.txid}');
      if (creditKeyPageResult.txid != null) {
        txIds.add(MapEntry('AddCredits', creditKeyPageResult.txid!));
      }
    } else {
      print('AddCredits FAILED: ${creditKeyPageResult.error}');
    }


    // =========================================================
    // Wait for Key Page Credits (WaitForCredits)
    // =========================================================
    print('Waiting for credits >= 1000...');
    for (var i = 0; i < 30; i++) {
      try {
        final result = await client.v3.rawCall('query', {
          'scope': '${createAdiUrl}/book/1',
          'query': {'queryType': 'default'},
        });
        final creditBalance = result['account']?['creditBalance'];
        if (creditBalance != null && (creditBalance as int) >= 1000) break;
      } catch (_) {}
      await Future.delayed(Duration(seconds: 2));
    }
    print('Credits reached');


    // =========================================================
    // Create Multi-Sig Key Book (Action: CreateKeyBook)
    // =========================================================
    final createMultiSigKeyBookUrl = '${createAdiUrl}/multisig-book';
    final createMultiSigKeyBookSigner = SmartSigner(
      client: client.v3,
      keypair: generateKeysSigner_1Key,
      signerUrl: '${createAdiUrl}/book/1',
    );
    final createMultiSigKeyBookResult = await createMultiSigKeyBookSigner.signSubmitAndWait(
      principal: createAdiUrl.toString(),
      body: TxBody.createKeyBook(
        url: createMultiSigKeyBookUrl,
        publicKeyHash: generateKeysSigner_1PubHash,
      ),
      memo: 'Create key book',
      maxAttempts: 30,
    );
    if (createMultiSigKeyBookResult.success) {
      print('CreateKeyBook SUCCESS - TxID: ${createMultiSigKeyBookResult.txid}');
      if (createMultiSigKeyBookResult.txid != null) {
        txIds.add(MapEntry('CreateKeyBook', createMultiSigKeyBookResult.txid!));
      }
    } else {
      print('CreateKeyBook FAILED: ${createMultiSigKeyBookResult.error}');
    }


    // =========================================================
    // Credit Multi-Sig Key Page (Action: AddCredits)
    // =========================================================
    final creditMultiSigKeyPageOracle = await client.v3.rawCall('network-status', {});
    final creditMultiSigKeyPageOraclePrice = creditMultiSigKeyPageOracle['oracle']['price'] as int;
    final creditMultiSigKeyPageSigner = SmartSigner(
      client: client.v3,
      keypair: generateKeysSigner_1Key,
      signerUrl: generateKeysSigner_1Lid.toString(),
    );
    final creditMultiSigKeyPageResult = await creditMultiSigKeyPageSigner.signSubmitAndWait(
      principal: generateKeysSigner_1Lta.toString(),
      body: TxBody.addCredits(
        recipient: '${createAdiUrl}/multisig-book/1',
        amount: '500000000',
        oracle: creditMultiSigKeyPageOraclePrice,
      ),
      memo: 'Add credits',
      maxAttempts: 30,
    );
    if (creditMultiSigKeyPageResult.success) {
      print('AddCredits SUCCESS - TxID: ${creditMultiSigKeyPageResult.txid}');
      if (creditMultiSigKeyPageResult.txid != null) {
        txIds.add(MapEntry('AddCredits', creditMultiSigKeyPageResult.txid!));
      }
    } else {
      print('AddCredits FAILED: ${creditMultiSigKeyPageResult.error}');
    }


    // =========================================================
    // Wait for Multi-Sig Credits (WaitForCredits)
    // =========================================================
    print('Waiting for credits >= 1000...');
    for (var i = 0; i < 30; i++) {
      try {
        final result = await client.v3.rawCall('query', {
          'scope': '${createAdiUrl}/multisig-book/1',
          'query': {'queryType': 'default'},
        });
        final creditBalance = result['account']?['creditBalance'];
        if (creditBalance != null && (creditBalance as int) >= 1000) break;
      } catch (_) {}
      await Future.delayed(Duration(seconds: 2));
    }
    print('Credits reached');


    // =========================================================
    // Generate Keys (Signer 2) (GenerateKeys)
    // =========================================================
    final generateKeysSigner_2Kp = await Ed25519KeyPair.generate();
    final generateKeysSigner_2Key = UnifiedKeyPair.fromEd25519(generateKeysSigner_2Kp);
    final generateKeysSigner_2Lid = await generateKeysSigner_2Kp.deriveLiteIdentityUrl();
    final generateKeysSigner_2Lta = await generateKeysSigner_2Kp.deriveLiteTokenAccountUrl();
    final generateKeysSigner_2PubKey = await generateKeysSigner_2Kp.publicKeyBytes();
    final generateKeysSigner_2PubHash = toHexString(sha256.convert(generateKeysSigner_2PubKey).bytes);
    print('Generated keypair:');
    print('  Lite Identity: ${generateKeysSigner_2Lid}');
    print('  Lite Token Account: ${generateKeysSigner_2Lta}');
    print('  Public Key Hash: ${generateKeysSigner_2PubHash}');


    // =========================================================
    // Add Signer 2 (Action: UpdateKeyPage)
    // =========================================================
    final addSigner_2Signer = SmartSigner(
      client: client.v3,
      keypair: generateKeysSigner_1Key,
      signerUrl: '${createAdiUrl}/multisig-book/1',
    );
    final addSigner_2RawOps = [{"type":"add","entry":{"keyHash":generateKeysSigner_2PubHash}}] as List;
    final addSigner_2Result = await addSigner_2Signer.signSubmitAndWait(
      principal: '${createAdiUrl}/multisig-book/1',
      body: TxBody.updateKeyPage(
        operations: addSigner_2RawOps
            .map((op) => KeyPageOperation.fromJson(Map<String, dynamic>.from(op)))
            .toList(),
      ),
      memo: 'Update key page',
      maxAttempts: 30,
    );
    if (addSigner_2Result.success) {
      print('UpdateKeyPage SUCCESS - TxID: ${addSigner_2Result.txid}');
      if (addSigner_2Result.txid != null) {
        txIds.add(MapEntry('UpdateKeyPage', addSigner_2Result.txid!));
      }
    } else {
      print('UpdateKeyPage FAILED: ${addSigner_2Result.error}');
    }


    // =========================================================
    // Generate Keys (Signer 3) (GenerateKeys)
    // =========================================================
    final generateKeysSigner_3Kp = await Ed25519KeyPair.generate();
    final generateKeysSigner_3Key = UnifiedKeyPair.fromEd25519(generateKeysSigner_3Kp);
    final generateKeysSigner_3Lid = await generateKeysSigner_3Kp.deriveLiteIdentityUrl();
    final generateKeysSigner_3Lta = await generateKeysSigner_3Kp.deriveLiteTokenAccountUrl();
    final generateKeysSigner_3PubKey = await generateKeysSigner_3Kp.publicKeyBytes();
    final generateKeysSigner_3PubHash = toHexString(sha256.convert(generateKeysSigner_3PubKey).bytes);
    print('Generated keypair:');
    print('  Lite Identity: ${generateKeysSigner_3Lid}');
    print('  Lite Token Account: ${generateKeysSigner_3Lta}');
    print('  Public Key Hash: ${generateKeysSigner_3PubHash}');


    // =========================================================
    // Add Signer 3 (Action: UpdateKeyPage)
    // =========================================================
    final addSigner_3Signer = SmartSigner(
      client: client.v3,
      keypair: generateKeysSigner_1Key,
      signerUrl: '${createAdiUrl}/multisig-book/1',
    );
    final addSigner_3RawOps = [{"type":"add","entry":{"keyHash":generateKeysSigner_3PubHash}}] as List;
    final addSigner_3Result = await addSigner_3Signer.signSubmitAndWait(
      principal: '${createAdiUrl}/multisig-book/1',
      body: TxBody.updateKeyPage(
        operations: addSigner_3RawOps
            .map((op) => KeyPageOperation.fromJson(Map<String, dynamic>.from(op)))
            .toList(),
      ),
      memo: 'Update key page',
      maxAttempts: 30,
    );
    if (addSigner_3Result.success) {
      print('UpdateKeyPage SUCCESS - TxID: ${addSigner_3Result.txid}');
      if (addSigner_3Result.txid != null) {
        txIds.add(MapEntry('UpdateKeyPage', addSigner_3Result.txid!));
      }
    } else {
      print('UpdateKeyPage FAILED: ${addSigner_3Result.error}');
    }


    // =========================================================
    // Set Threshold (2 of 3) (Action: UpdateKeyPage)
    // =========================================================
    final setThreshold_2Of_3Signer = SmartSigner(
      client: client.v3,
      keypair: generateKeysSigner_1Key,
      signerUrl: '${createAdiUrl}/multisig-book/1',
    );
    final setThreshold_2Of_3RawOps = [{"type":"setThreshold","threshold":2}] as List;
    final setThreshold_2Of_3Result = await setThreshold_2Of_3Signer.signSubmitAndWait(
      principal: '${createAdiUrl}/multisig-book/1',
      body: TxBody.updateKeyPage(
        operations: setThreshold_2Of_3RawOps
            .map((op) => KeyPageOperation.fromJson(Map<String, dynamic>.from(op)))
            .toList(),
      ),
      memo: 'Update key page',
      maxAttempts: 30,
    );
    if (setThreshold_2Of_3Result.success) {
      print('UpdateKeyPage SUCCESS - TxID: ${setThreshold_2Of_3Result.txid}');
      if (setThreshold_2Of_3Result.txid != null) {
        txIds.add(MapEntry('UpdateKeyPage', setThreshold_2Of_3Result.txid!));
      }
    } else {
      print('UpdateKeyPage FAILED: ${setThreshold_2Of_3Result.error}');
    }


    // =========================================================
    // Raise Threshold to 3 (2-of-3 signatures) (Action: CoSign — update_key_page under an M-of-N threshold)
    // =========================================================
    // A threshold needs DISTINCT keys signing the SAME transaction. Signing the
    // body twice does not work: the first signature's metadata becomes the
    // transaction's `initiator` and is baked into the header, so a second
    // independent signature is over a different transaction hash and neither copy
    // reaches the threshold. Co-sign the existing envelope instead.
    final raiseThresholdTo_3_2Of_3SignaturesBody = TxBody.updateKeyPage(operations: ([{"type":"setThreshold","threshold":3}] as List).map((op) => KeyPageOperation.fromJson(Map<String, dynamic>.from(op))).toList());
    final raiseThresholdTo_3_2Of_3SignaturesSigner = SmartSigner(
      client: client.v3,
      keypair: generateKeysSigner_1Key,
      signerUrl: '${createAdiUrl}/multisig-book/1',
    );
    var raiseThresholdTo_3_2Of_3SignaturesEnvelope = await raiseThresholdTo_3_2Of_3SignaturesSigner.sign(
      principal: '${createAdiUrl}/multisig-book/1',
      body: raiseThresholdTo_3_2Of_3SignaturesBody,
    );
    // Co-signer 0: appends a signature to the SAME transaction hash.
    raiseThresholdTo_3_2Of_3SignaturesEnvelope = await SmartSigner(
      client: client.v3,
      keypair: generateKeysSigner_2Key,
      signerUrl: '${createAdiUrl}/multisig-book/1',
    ).signExisting(raiseThresholdTo_3_2Of_3SignaturesEnvelope);
    // Submit only once every signature is collected: resubmitting a signature that
    // is already on chain trips replay protection.
    final raiseThresholdTo_3_2Of_3SignaturesResult = await client.v3.submit(raiseThresholdTo_3_2Of_3SignaturesEnvelope.toJson());
    final raiseThresholdTo_3_2Of_3SignaturesMessages =
        raiseThresholdTo_3_2Of_3SignaturesResult is List ? raiseThresholdTo_3_2Of_3SignaturesResult : [raiseThresholdTo_3_2Of_3SignaturesResult];
    final raiseThresholdTo_3_2Of_3SignaturesRejected = raiseThresholdTo_3_2Of_3SignaturesMessages.any((m) {
      final status = (m is Map) ? m['status'] : null;
      return status is Map && (status['failed'] == true || status['error'] != null);
    });
    // The transaction's own txID is the first one reported; the rest belong to
    // the signature messages.
    final raiseThresholdTo_3_2Of_3SignaturesTxid = raiseThresholdTo_3_2Of_3SignaturesMessages
        .map((m) => (m is Map && m['status'] is Map) ? m['status']['txID'] : null)
        .firstWhere((t) => t != null, orElse: () => '') as String;
    if (raiseThresholdTo_3_2Of_3SignaturesRejected) {
      print('CoSign update_key_page FAILED: ${raiseThresholdTo_3_2Of_3SignaturesResult}');
    } else {
      // Acceptance is not execution. A transaction that has not reached its
      // threshold is accepted with code "ok" and then sits pending, so the only
      // honest confirmation is the delivered status.
      var raiseThresholdTo_3_2Of_3SignaturesStatus = 'unknown';
      for (var i = 0; i < 30; i++) {
        try {
          final q = await client.v3.query({'scope': raiseThresholdTo_3_2Of_3SignaturesTxid});
          final raw = (q is Map) ? q['status'] : null;
          if (raw is String) {
            raiseThresholdTo_3_2Of_3SignaturesStatus = raw;
            if (raiseThresholdTo_3_2Of_3SignaturesStatus != 'pending') break;
          }
        } catch (_) {
          // not indexed yet
        }
        await Future<void>.delayed(const Duration(seconds: 2));
      }
      if (raiseThresholdTo_3_2Of_3SignaturesStatus == 'delivered') {
        print('CoSign update_key_page DELIVERED with 2 signature(s) - TxID: ${raiseThresholdTo_3_2Of_3SignaturesTxid}');
      } else {
        print('CoSign update_key_page NOT DELIVERED (status=${raiseThresholdTo_3_2Of_3SignaturesStatus}) - TxID: ${raiseThresholdTo_3_2Of_3SignaturesTxid}');
      }
    }


    print('\n========== Transaction Summary ==========');
    for (final entry in txIds) {
      print('  ${entry.key}: ${entry.value}');
    }
    print('Flow completed successfully!');
  } finally {
    client.close();
  }
}
