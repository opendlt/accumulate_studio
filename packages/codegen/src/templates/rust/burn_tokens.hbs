    // =========================================================
    // {{label}} (Action: BurnTokens)
    // =========================================================
    let mut {{varName}}_signer = SmartSigner::new(&client, {{keyVarName}}_signer.clone(), {{#if signerUrlIsRef}}&{{signerUrl}}{{else}}{{signerUrl}}{{/if}});
    let {{varName}}_result = {{varName}}_signer.sign_submit_and_wait(
        {{#if burnTokensPrincipalIsRef}}&{{burnTokensPrincipal}}{{else}}"{{burnTokensPrincipal}}"{{/if}},
        &TxBody::burn_tokens("{{burnAmount}}"),
        Some("Burn tokens"),
        30,
    ).await;
    if {{varName}}_result.success {
        println!("BurnTokens SUCCESS - TxID: {:?}", {{varName}}_result.txid);
        if let Some(ref txid) = {{varName}}_result.txid {
            tx_ids.push(("BurnTokens", txid.clone()));
        }
    } else {
        println!("BurnTokens FAILED: {:?}", {{varName}}_result.error);
    }