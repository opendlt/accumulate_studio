    // =========================================================
    // {{label}} (Action: TransferCredits)
    // =========================================================
    let mut {{varName}}_signer = SmartSigner::new(&client, {{keyVarName}}_signer.clone(), {{#if signerUrlIsRef}}&{{signerUrl}}{{else}}{{signerUrl}}{{/if}});
    let {{varName}}_result = {{varName}}_signer.sign_submit_and_wait(
        {{#if transferCreditsPrincipalIsRef}}&{{transferCreditsPrincipal}}{{else}}"{{transferCreditsPrincipal}}"{{/if}},
        &TxBody::transfer_credits({{#if transferCreditsRecipientIsRef}}&{{transferCreditsRecipient}}{{else}}"{{transferCreditsRecipient}}"{{/if}}, {{transferCreditsAmount}}),
        Some("Transfer credits"),
        30,
    ).await;
    if {{varName}}_result.success {
        println!("TransferCredits SUCCESS - TxID: {:?}", {{varName}}_result.txid);
        if let Some(ref txid) = {{varName}}_result.txid {
            tx_ids.push(("TransferCredits", txid.clone()));
        }
    } else {
        println!("TransferCredits FAILED: {:?}", {{varName}}_result.error);
    }