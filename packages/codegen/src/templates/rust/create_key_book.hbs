    // =========================================================
    // {{label}} (Action: CreateKeyBook)
    // =========================================================
    let {{varName}}_url = {{#if keyBookUrlIsRef}}{{keyBookUrl}}{{else}}"{{keyBookUrl}}".to_string(){{/if}};
    let mut {{varName}}_signer = SmartSigner::new(&client, {{keyVarName}}_signer.clone(), {{#if signerUrlIsRef}}&{{signerUrl}}{{else}}{{signerUrl}}{{/if}});
    let {{varName}}_result = {{varName}}_signer.sign_submit_and_wait(
        {{#if keyBookPrincipalIsRef}}&{{keyBookPrincipal}}{{else}}"{{keyBookPrincipal}}"{{/if}},
        &TxBody::create_key_book(&{{varName}}_url, &{{keyBookKeyHash}}),
        Some("Create key book"),
        30,
    ).await;
    if {{varName}}_result.success {
        println!("CreateKeyBook SUCCESS - TxID: {:?}", {{varName}}_result.txid);
        if let Some(ref txid) = {{varName}}_result.txid {
            tx_ids.push(("CreateKeyBook", txid.clone()));
        }
    } else {
        println!("CreateKeyBook FAILED: {:?}", {{varName}}_result.error);
    }