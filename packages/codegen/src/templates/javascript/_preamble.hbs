/**
 * {{flow.name}}
 * Generated by Accumulate Studio
 * Network: {{network}}
 *
 * Install: npm install accumulate.js
 * Run with: node script.mjs
 */

import {
    Accumulate,
    SmartSigner,
    TxBody,
    Ed25519KeyPair,
    KERMIT_V2,
    KERMIT_V3,
{{#if hasWait}}
    pollForBalance,
    pollForCredits,
{{/if}}
} from "accumulate.js";

const V2_ENDPOINT = process.env.ACCUMULATE_V2_URL || KERMIT_V2;
const V3_ENDPOINT = process.env.ACCUMULATE_V3_URL || KERMIT_V3;

async function main() {
    const client = Accumulate.custom(V2_ENDPOINT, V3_ENDPOINT);
    const txIds = [];
    try {
{{#if hasFlowVars}}
{{#each flowVarDefs}}
        const {{this.pythonName}} = {{this.jsDefaultValue}};  // {{this.description}}
{{/each}}
{{/if}}
