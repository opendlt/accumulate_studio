        // =========================================================
        // {{label}} (Action: CreateToken)
        // =========================================================
        const {{varName}}Url = {{#if tokenIssuerUrlIsRef}}{{tokenIssuerUrl}}{{else}}"{{tokenIssuerUrl}}"{{/if}};
        const {{varName}}Signer = new SmartSigner(client, {{keyVarName}}Kp.toKey(), {{signerUrl}});
        const {{varName}}Result = await {{varName}}Signer.signSubmitAndWait(
            {{#if createTokenPrincipalIsRef}}{{createTokenPrincipal}}{{else}}"{{createTokenPrincipal}}"{{/if}},
            TxBody.createToken(
                {{#if tokenIssuerUrlIsRef}}{{tokenIssuerUrl}}{{else}}"{{tokenIssuerUrl}}"{{/if}},
                {{#if tokenSymbolIsRef}}{{tokenSymbol}}{{else}}"{{tokenSymbol}}"{{/if}},
                {{tokenPrecision}},
            ),
        );
        if ({{varName}}Result.success) {
            console.log(`CreateToken SUCCESS - TxID: ${ {{~varName}}Result.txid}`);
            if ({{varName}}Result.txid) txIds.push(["CreateToken", {{varName}}Result.txid]);
        } else {
            console.log(`CreateToken FAILED: ${ {{~varName}}Result.error}`);
        }
