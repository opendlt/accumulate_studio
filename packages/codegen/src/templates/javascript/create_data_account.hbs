        // =========================================================
        // {{label}} (Action: CreateDataAccount)
        // =========================================================
        const {{varName}}Url = {{#if dataAccUrlIsRef}}{{dataAccUrl}}{{else}}"{{dataAccUrl}}"{{/if}};
        const {{varName}}Signer = new SmartSigner(client, {{keyVarName}}Kp.toKey(), {{signerUrl}});
        const {{varName}}Result = await {{varName}}Signer.signSubmitAndWait(
            {{#if dataAccPrincipalIsRef}}{{dataAccPrincipal}}{{else}}"{{dataAccPrincipal}}"{{/if}},
            TxBody.createDataAccount({{#if dataAccUrlIsRef}}{{dataAccUrl}}{{else}}"{{dataAccUrl}}"{{/if}}),
        );
        if ({{varName}}Result.success) {
            console.log(`CreateDataAccount SUCCESS - TxID: ${ {{~varName}}Result.txid}`);
            if ({{varName}}Result.txid) txIds.push(["CreateDataAccount", {{varName}}Result.txid]);
        } else {
            console.log(`CreateDataAccount FAILED: ${ {{~varName}}Result.error}`);
        }
