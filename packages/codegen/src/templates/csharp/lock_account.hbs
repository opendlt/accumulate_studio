            // =========================================================
            // {{label}} (Action: LockAccount)
            // =========================================================
            var {{varName}}Signer = new SmartSigner(client.V3, {{keyVarName}}Kp, {{signerUrl}});
            var {{varName}}Result = await {{varName}}Signer.SignSubmitAndWaitAsync(
                {{lockAccountPrincipal}},
                TxBody.LockAccount({{lockHeight}})
            );
            if ({{varName}}Result.Success)
            {
                Console.WriteLine($"LockAccount SUCCESS - TxID: { {{~varName}}Result.TxId}");
                if ({{varName}}Result.TxId != null) txIds.Add(("LockAccount", {{varName}}Result.TxId));
            }
            else
            {
                Console.WriteLine($"LockAccount FAILED: { {{~varName}}Result.Error}");
            }
