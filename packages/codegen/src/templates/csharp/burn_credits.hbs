            // =========================================================
            // {{label}} (Action: BurnCredits)
            // =========================================================
            var {{varName}}Signer = new SmartSigner(client.V3, {{keyVarName}}Kp, {{signerUrl}});
            var {{varName}}Result = await {{varName}}Signer.SignSubmitAndWaitAsync(
                {{burnCreditsPrincipal}},
                TxBody.BurnCredits({{burnCreditsAmount}})
            );
            if ({{varName}}Result.Success)
            {
                Console.WriteLine($"BurnCredits SUCCESS - TxID: { {{~varName}}Result.TxId}");
                if ({{varName}}Result.TxId != null) txIds.Add(("BurnCredits", {{varName}}Result.TxId));
            }
            else
            {
                Console.WriteLine($"BurnCredits FAILED: { {{~varName}}Result.Error}");
            }
