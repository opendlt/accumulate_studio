            // =========================================================
            // {{label}} (Action: UpdateKey)
            // =========================================================
            var {{varName}}Signer = new SmartSigner(client.V3, {{keyVarName}}Kp, {{signerUrl}});
            var {{varName}}Result = await {{varName}}Signer.SignSubmitAndWaitAsync(
                {{updateKeyPrincipal}},
                TxBody.UpdateKey({{#if updateKeyNewKeyIsRef}}{{updateKeyNewKey}}{{else}}"{{updateKeyNewKey}}"{{/if}})
            );
            if ({{varName}}Result.Success)
            {
                Console.WriteLine($"UpdateKey SUCCESS - TxID: { {{~varName}}Result.TxId}");
                if ({{varName}}Result.TxId != null) txIds.Add(("UpdateKey", {{varName}}Result.TxId));
            }
            else
            {
                Console.WriteLine($"UpdateKey FAILED: { {{~varName}}Result.Error}");
            }
