            // =========================================================
            // {{label}} (CreateLiteTokenAccount)
            // =========================================================
            // Lite token accounts are automatically derived from the keypair
            // No explicit creation transaction needed
            Console.WriteLine($"Lite Token Account: { {{~keyVarName}}Lta.String()}");
