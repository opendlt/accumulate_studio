            // =========================================================
            // {{label}} (Action: BurnTokens)
            // =========================================================
            var {{varName}}Signer = new SmartSigner(client.V3, {{keyVarName}}Kp, {{signerUrl}});
            var {{varName}}Result = await {{varName}}Signer.SignSubmitAndWaitAsync(
                {{burnTokensPrincipal}},
                TxBody.BurnTokens("{{burnAmount}}")
            );
            if ({{varName}}Result.Success)
            {
                Console.WriteLine($"BurnTokens SUCCESS - TxID: { {{~varName}}Result.TxId}");
                if ({{varName}}Result.TxId != null) txIds.Add(("BurnTokens", {{varName}}Result.TxId));
            }
            else
            {
                Console.WriteLine($"BurnTokens FAILED: { {{~varName}}Result.Error}");
            }
