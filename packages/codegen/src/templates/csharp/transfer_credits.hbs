            // =========================================================
            // {{label}} (Action: TransferCredits)
            // =========================================================
            var {{varName}}Signer = new SmartSigner(client.V3, {{keyVarName}}Kp, {{signerUrl}});
            var {{varName}}Result = await {{varName}}Signer.SignSubmitAndWaitAsync(
                {{transferCreditsPrincipal}},
                TxBody.TransferCredits(
                    {{#if transferCreditsRecipientIsRef}}{{transferCreditsRecipient}}{{else}}"{{transferCreditsRecipient}}"{{/if}},
                    {{transferCreditsAmount}}
                )
            );
            if ({{varName}}Result.Success)
            {
                Console.WriteLine($"TransferCredits SUCCESS - TxID: { {{~varName}}Result.TxId}");
                if ({{varName}}Result.TxId != null) txIds.Add(("TransferCredits", {{varName}}Result.TxId));
            }
            else
            {
                Console.WriteLine($"TransferCredits FAILED: { {{~varName}}Result.Error}");
            }
