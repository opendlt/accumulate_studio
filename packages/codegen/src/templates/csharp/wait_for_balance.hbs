            // =========================================================
            // {{label}} (WaitForBalance)
            // =========================================================
            Console.WriteLine("Waiting for balance >= {{minBalance}}...");
            await helper.PollForBalanceAsync({{waitAccount}});
            Console.WriteLine("Balance reached");
