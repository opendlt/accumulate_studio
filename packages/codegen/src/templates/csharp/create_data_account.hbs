            // =========================================================
            // {{label}} (Action: CreateDataAccount)
            // =========================================================
{{#if dataAccUrlIsRef}}
            var {{varName}}Url = {{dataAccUrl}};
{{else}}
            var {{varName}}Url = "{{dataAccUrl}}";
{{/if}}
            var {{varName}}Signer = new SmartSigner(client.V3, {{keyVarName}}Kp, {{signerUrl}});
            var {{varName}}Result = await {{varName}}Signer.SignSubmitAndWaitAsync(
                {{dataAccPrincipal}},
                TxBody.CreateDataAccount({{#if dataAccUrlIsRef}}{{dataAccUrl}}{{else}}"{{dataAccUrl}}"{{/if}})
            );
            if ({{varName}}Result.Success)
            {
                Console.WriteLine($"CreateDataAccount SUCCESS - TxID: { {{~varName}}Result.TxId}");
                if ({{varName}}Result.TxId != null) txIds.Add(("CreateDataAccount", {{varName}}Result.TxId));
            }
            else
            {
                Console.WriteLine($"CreateDataAccount FAILED: { {{~varName}}Result.Error}");
            }
