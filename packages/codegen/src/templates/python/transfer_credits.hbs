        # =========================================================
        # {{label}} (Action: TransferCredits)
        # =========================================================
        {{varName}}_signer = SmartSigner(client.v3, {{keyVarName}}_kp, {{signerUrl}})
        {{varName}}_result = {{varName}}_signer.sign_submit_and_wait(
            principal="{{transferCreditsPrincipal}}",
            body=TxBody.transfer_credits(
                to="{{transferCreditsRecipient}}",
                amount={{transferCreditsAmount}},
            ),
            max_attempts=30,
        )
        if {{varName}}_result.success:
            print(f"TransferCredits SUCCESS - TxID: { {{~varName}}_result.txid}")
            tx_ids.append(("TransferCredits", {{varName}}_result.txid))
        else:
            print(f"TransferCredits FAILED: { {{~varName}}_result.error}")