        # =========================================================
        # {{label}} (Action: Faucet)
        # =========================================================
        fund_account(client, {{faucetAccount}}, faucet_requests={{faucetTimes}})
        print("Faucet request completed")