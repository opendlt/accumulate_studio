        # =========================================================
        # {{label}} (Action: BurnTokens)
        # =========================================================
        {{varName}}_signer = SmartSigner(client.v3, {{keyVarName}}_kp, {{signerUrl}})
        {{varName}}_result = {{varName}}_signer.sign_submit_and_wait(
            principal={{#if burnTokensPrincipalIsRef}}{{burnTokensPrincipal}}{{else}}"{{burnTokensPrincipal}}"{{/if}},
            body=TxBody.burn_tokens(
                amount='{{burnAmount}}',
            ),
            max_attempts=30,
        )
        if {{varName}}_result.success:
            print(f"BurnTokens SUCCESS - TxID: { {{~varName}}_result.txid}")
            tx_ids.append(("BurnTokens", {{varName}}_result.txid))
        else:
            print(f"BurnTokens FAILED: { {{~varName}}_result.error}")