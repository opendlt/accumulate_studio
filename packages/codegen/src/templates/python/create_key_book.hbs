        # =========================================================
        # {{label}} (Action: CreateKeyBook)
        # =========================================================
        {{varName}}_url = {{#if keyBookUrlIsRef}}{{keyBookUrl}}{{else}}"{{keyBookUrl}}"{{/if}}
        {{varName}}_signer = SmartSigner(client.v3, {{keyVarName}}_kp, {{signerUrl}})
        {{varName}}_result = {{varName}}_signer.sign_submit_and_wait(
            principal={{#if keyBookPrincipalIsRef}}{{keyBookPrincipal}}{{else}}"{{keyBookPrincipal}}"{{/if}},
            body=TxBody.create_key_book(
                url={{varName}}_url,
                public_key_hash={{keyBookKeyHash}},
            ),
            max_attempts=30,
        )
        if {{varName}}_result.success:
            print(f"CreateKeyBook SUCCESS - TxID: { {{~varName}}_result.txid}")
            tx_ids.append(("CreateKeyBook", {{varName}}_result.txid))
        else:
            print(f"CreateKeyBook FAILED: { {{~varName}}_result.error}")