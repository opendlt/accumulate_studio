    // =========================================================
    // {{label}} (Action: WriteData)
    // =========================================================
    final {{varName}}Signer = SmartSigner(
      client: client.v3,
      keypair: {{keyVarName}}Key,
      signerUrl: {{signerUrl}},
    );
    final {{varName}}Result = await {{varName}}Signer.signSubmitAndWait(
      principal: {{writeDataPrincipal}},
      body: TxBody.writeData(entriesHex: [{{entriesExpr}}]),
      memo: 'Write data',
      maxAttempts: 30,
    );
    if ({{varName}}Result.success) {
      print('WriteData SUCCESS - TxID: ${ {{~varName}}Result.txid}');
      if ({{varName}}Result.txid != null) {
        txIds.add(MapEntry('WriteData', {{varName}}Result.txid!));
      }
    } else {
      print('WriteData FAILED: ${ {{~varName}}Result.error}');
    }
