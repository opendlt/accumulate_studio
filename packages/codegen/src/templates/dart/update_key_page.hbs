    // =========================================================
    // {{label}} (Action: UpdateKeyPage)
    // =========================================================
    final {{varName}}Signer = SmartSigner(
      client: client.v3,
      keypair: {{keyVarName}}Key,
      signerUrl: {{signerUrl}},
    );
    final {{varName}}RawOps = {{updateKeyPageOps}} as List;
    final {{varName}}Result = await {{varName}}Signer.signSubmitAndWait(
      principal: {{updateKeyPagePrincipal}},
      body: TxBody.updateKeyPage(
        operations: {{varName}}RawOps
            .map((op) => KeyPageOperation.fromJson(Map<String, dynamic>.from(op)))
            .toList(),
      ),
      memo: 'Update key page',
      maxAttempts: 30,
    );
    if ({{varName}}Result.success) {
      print('UpdateKeyPage SUCCESS - TxID: ${ {{~varName}}Result.txid}');
      if ({{varName}}Result.txid != null) {
        txIds.add(MapEntry('UpdateKeyPage', {{varName}}Result.txid!));
      }
    } else {
      print('UpdateKeyPage FAILED: ${ {{~varName}}Result.error}');
    }
